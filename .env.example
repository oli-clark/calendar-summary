# Google Calendar API Configuration
# Path to your OAuth2 credentials file (download from Google Cloud Console)
GOOGLE_CALENDAR_CREDENTIALS_PATH=./credentials.json
# Path where the OAuth2 token will be stored after first authentication
GOOGLE_CALENDAR_TOKEN_PATH=./token.json
# Calendar ID to fetch events from (use 'primary' for main calendar)
CALENDAR_ID=primary

# Claude API Configuration
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-...

# Twilio WhatsApp Configuration
# Get these from: https://console.twilio.com/
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...
# Twilio WhatsApp sandbox number (default shown below)
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
# Your WhatsApp number (must be joined to Twilio sandbox)
TWILIO_WHATSAPP_TO=whatsapp:+44...

# Optional Configuration
# Set to 'true' to print summary without sending WhatsApp message
DRY_RUN=false
# Enable verbose logging for debugging
VERBOSE=false
